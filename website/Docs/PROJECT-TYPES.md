# 📁 Типы проектов в портфолио

> 💡 **Полная документация:** [PROJECT-TYPES-GUIDE.md](./PROJECT-TYPES-GUIDE.md)

В портфолио поддерживается **10 типов проектов** с уникальными иконками и отображением.

## 🎯 Все типы

| Тип | Иконка | Метка | Использование |
|-----|--------|-------|---------------|
| `website` | 🌐 | Веб-сайт | Сайты, лендинги, веб-приложения |
| `mobile-app` | 📱 | Мобильное приложение | iOS, Android (React Native, Flutter, Swift, Kotlin) |
| `telegram-bot` | 🤖 | Telegram бот | Боты для Telegram (Python, Node.js) |
| `game` | 🎮 | Игра | Игры (Unity, Godot, Construct 3) |
| `desktop-app` | 💻 | Десктоп приложение | Windows, macOS, Linux приложения |
| `library` | 📦 | Библиотека | NPM пакеты, SDK, фреймворки |
| `tool` | 🔧 | Инструмент | CLI утилиты, DevTools |
| `template` | 📋 | Шаблон | Стартеры, бойлерплейты |
| `api` | 🔌 | API/Backend | REST API, GraphQL, микросервисы |
| `other` | 📁 | Другое | Прочие проекты |

## 📝 Быстрый старт

### Создание проекта через CLI

```bash
npm run projects
# Выбрать "1) Создать новый проект"
```

При выборе типа увидишь красивое меню с иконками:

```
? Тип проекта

  ▶ 1. 🌐 Веб-сайт
    2. 📱 Мобильное приложение
    3. 🤖 Telegram бот
    4. 🎮 Игра
    5. 💻 Десктоп приложение
    6. 📦 Библиотека
    7. 🔧 Инструмент
    8. 📋 Шаблон
    9. 🔌 API/Backend
    10. 📁 Другое

Введи номер, название или нажми Enter
Выбор [website]: 
```

### Создание вручную

```json
{
  "title": "Мой проект",
  "type": "mobile-app",
  "tags": ["React Native", "TypeScript"],
  "status": "completed",
  ...
}
```

## 🎨 Отображение в UI

### ProjectCard
- **Иконка типа** - отображается в бейдже типа проекта
- **Фильтрация** - можно фильтровать проекты по типу

### ProjectDetail
- **Метка типа** - показывается рядом со статусом
- **Структурированные данные** - правильный Schema.org тип

### Фильтр по типам
Компонент `ProjectTypeFilters` автоматически появляется на странице `/projects` если есть больше одного типа проектов.

## 📝 Использование в CLI

При создании нового проекта через `npm run projects`:

```
? Тип проекта ([website] / mobile-app / telegram-bot / game / desktop-app / library / tool / template / api / other): 
```

Примеры:
- Для веб-сайта: `website` (по умолчанию)
- Для Telegram бота: `telegram-bot`
- Для игры: `game`
- Для мобильного приложения: `mobile-app`

## 🔄 Миграция существующих проектов

Старые типы автоматически поддерживаются:
- `app` → используй `mobile-app` или `desktop-app`
- Просто измени поле `type` в JSON файле проекта

Пример:
```json
{
  "title": "Biogram",
  "type": "telegram-bot",  // ← было "other"
  ...
}
```

## 🏷️ JSON-LD Schema Types

Каждый тип проекта маппится на правильный Schema.org тип:

| Project Type | Schema.org Type |
|--------------|-----------------|
| `website` | `WebSite` |
| `mobile-app` | `MobileApplication` |
| `telegram-bot` | `SoftwareApplication` |
| `game` | `VideoGame` |
| `desktop-app` | `SoftwareApplication` |
| `library` | `SoftwareSourceCode` |
| `tool` | `SoftwareApplication` |
| `template` | `CreativeWork` |
| `api` | `WebAPI` |
| `other` | `CreativeWork` |

Это помогает поисковым системам правильно индексировать проекты.
