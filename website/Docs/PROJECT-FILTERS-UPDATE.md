# ✨ Обновление фильтров проектов

## 🎨 Что изменилось

### Новый дизайн табов-фильтров
Полностью переработан компонент `ProjectTypeFilters` в стиле **glassmorphism**:

- ✅ **Полноширинные вкладки** - занимают всю ширину контейнера
- ✅ **Равномерное распределение** - все вкладки одинаковой ширины
- ✅ **Glassmorphism эффекты** - фирменный стиль с blur и прозрачностью
- ✅ **Плавные переходы** - анимация при переключении
- ✅ **Градиентная подсветка** - активная вкладка подсвечивается зелёным градиентом
- ✅ **Адаптивность** - на мобильных скроллится горизонтально

### Соответствие Brand Identity

Компонент следует дизайн-токенам из `.github/skills/brand-identity/`:

```css
/* Фон */
background: var(--color-bg-glass)
backdrop-filter: blur(12px)

/* Границы */
border: 1px solid var(--color-border-glass)

/* Активная вкладка */
background: linear-gradient(180deg, rgba(16, 185, 129, 0.15) 0%, rgba(16, 185, 129, 0.05) 100%)

/* Индикатор */
bottom-border: var(--color-primary) with glow
box-shadow: 0 0 20px -5px var(--color-primary-glow)
```

## 📱 Адаптивность

### Desktop (≥640px)
- Вкладки распределены по ширине: `grid grid-flow-col auto-cols-fr`
- Показываются иконки + текст

### Mobile (<640px)
- Горизонтальный скролл: `flex overflow-x-auto scrollbar-hide`
- Только иконки на очень малых экранах
- Иконки + текст на экранах ≥480px

## 🎯 Типы проектов

Все 10 типов с иконками:

| Тип | Иконка | Метка |
|-----|--------|-------|
| all | 🎯 | Все проекты |
| website | 🌐 | Веб-сайты |
| mobile-app | 📱 | Мобильные |
| telegram-bot | 🤖 | Telegram |
| game | 🎮 | Игры |
| desktop-app | 💻 | Десктоп |
| library | 📦 | Библиотеки |
| tool | 🔧 | Инструменты |
| template | 📋 | Шаблоны |
| api | 🔌 | API |
| other | 📁 | Другое |

## 🔧 Технические детали

### Компоненты
- `ProjectTypeFilters.tsx` - React компонент с фильтрацией
- Загружается с `client:load` для немедленной интерактивности

### Фильтрация
- URL параметр `?type=mobile-app` для прямых ссылок
- Custom event `projectTypeFilterChange` для синхронизации
- Показ/скрытие через `display: none` на wrapper элементах

### Стили
- Добавлен `.scrollbar-hide` в `global.css`
- Использует CSS переменные из темы
- Плавные transitions: `duration-300 ease-out`

## 🚀 Использование

```tsx
<ProjectTypeFilters 
  client:load
  types={projectTypes}
/>
```

Компонент автоматически:
- Добавляет вкладку "Все проекты"
- Читает тип из URL при загрузке
- Обновляет URL при переключении
- Диспатчит события для фильтрации грида

## 📊 Статус проектов

Все проекты корректно типизированы:

- ✅ 2 веб-сайта (`website`)
- ✅ 2 мобильных приложения (`mobile-app`)
- ✅ 1 десктоп приложение (`desktop-app`)
- ✅ 1 Telegram бот (`telegram-bot`)
- ✅ 1 игра (`game`)

Итого: **7 проектов** с правильными типами.

## 🎨 Preview

```
┌─────────────────────────────────────────────────────────────┐
│ 🎯 Все проекты │ 🌐 Веб-сайты │ 📱 Мобильные │ 🤖 Telegram │
├─────────────────────────────────────────────────────────────┤
│                        [активная подсветка]                  │
└─────────────────────────────────────────────────────────────┘
```

Вкладка "Все проекты" активна по умолчанию с зелёным градиентом и glow эффектом.
