# 📦 Инструкция по добавлению Assets в Art Frost Portfolio

> **Версия:** 1.0  
> **Дата:** 26 января 2026  
> **Статус:** Готово к использованию

---

## 📋 Содержание

1. [3D Assets — Куда класть и как они работают](#-3d-assets)
2. [OG Images — Open Graph для соцсетей](#-og-images-open-graph)
3. [Project Images — Скриншоты проектов](#-project-images)
4. [Favicon & Icons](#-favicon--icons)
5. [Self-hosted Fonts](#-self-hosted-fonts)
6. [Оптимизация изображений](#-оптимизация-изображений)

---

## 🎨 3D Assets

### Структура папок

```
website/public/assets/3d/
├── hero-star.webp              ← Hero section (главная)
├── avatar-sphere.webp          ← About page (страница обо мне)
├── floating-layers.webp        ← Value proposition (будущее)
├── code-cube.webp              ← Services Dev (будущее)
├── fluid-droplet.webp          ← Services Design (будущее)
└── communication-orb.webp      ← CTA section (будущее)
```

### Технические требования

| Параметр | Значение |
|----------|----------|
| **Формат** | WebP (или AVIF для еще лучшей оптимизации) |
| **Размер файла** | < 200 KB (идеально < 100 KB) |
| **Разрешение** | 800×800 px — 1200×1200 px |
| **Прозрачный фон** | Да (PNG → WebP с альфа-каналом) |
| **Цветовая схема** | Зеленый (#10B981) + темные тона |

### Описание каждого 3D-ассета

#### 1. `hero-star.webp` — Hero Section

**Где используется:** Главная страница (`/`)  
**Компонент:** `src/components/home/Hero.astro`  
**Описание:** 6-8 конечная 3D-звезда или геометрический узел. Металлический зеленый цвет, мягкие края.  
**Стиль:** Футуристичный, hi-tech, минималистичный  
**Анимация:** Медленное вращение при скролле (уже реализовано через ScrollFrameAnimation)

**Текущее состояние:** ✅ Используется ScrollFrameAnimation с 36 кадрами  
**Что делать:** Ничего (уже работает), но можно заменить статичным изображением `hero-star.webp` для упрощения

---

#### 2. `avatar-sphere.webp` — About Page

**Где используется:** Страница "Обо мне" (`/about`)  
**Компонент:** Будет добавлен в `src/components/about/AboutHero.astro`  
**Описание:** Абстрактный 3D-аватар — стилизованная стеклянная голова/креативная сфера с частицами инея  
**Стиль:** Glassmorphism, полупрозрачный, художественный  
**Анимация:** Медленное парение (`animate-float-slow`)

**✨ Автоматическое использование:**
```astro
<!-- В AboutHero.astro будет добавлено -->
<Asset3D 
  name="avatar-sphere" 
  alt="Abstract 3D avatar" 
  animation="float-slow"
  glowIntensity="medium"
  class="max-w-md mx-auto"
/>
```

---

#### 3. `floating-layers.webp` — Value Proposition (будущее)

**Где используется:** Секция ценностного предложения (пока не реализована)  
**Описание:** Стопка стеклянных листов/интерфейсных панелей с глубиной  
**Стиль:** Frosted glass, depth, layered  
**Анимация:** Subtle parallax on scroll

---

#### 4. `code-cube.webp` — Services Dev (будущее)

**Где используется:** Секция "Разработка"  
**Описание:** Глянцевый куб с выгравированными `{}` скобками  
**Стиль:** Glowing embossed, tech-inspired  

---

#### 5. `fluid-droplet.webp` — Services Design (будущее)

**Где используется:** Секция "Дизайн"  
**Описание:** Металлическая жидкая форма, символизирует креативность и поток  
**Стиль:** Liquid metal, organic shape

---

#### 6. `communication-orb.webp` — CTA Section (будущее)

**Где используется:** Call-to-action секция (контакты)  
**Описание:** Светящаяся сфера или 3D-конверт почты  
**Стиль:** Glass texture, glowing core

---

### Как использовать 3D-ассеты в коде

**Простой способ (рекомендуется):**

```astro
---
import Asset3D from '@components/ui/Asset3D.astro';
---

<!-- Автоматически загружает /assets/3d/hero-star.webp -->
<Asset3D name="hero-star" alt="" />

<!-- С анимацией и эффектами -->
<Asset3D 
  name="avatar-sphere" 
  alt="Avatar" 
  animation="float"
  glowIntensity="high"
  class="max-w-lg"
/>
```

**Параметры компонента `<Asset3D />`:**

| Проп | Тип | Описание |
|------|-----|----------|
| `name` | `string` | Имя файла без расширения (например, `"hero-star"`) |
| `alt` | `string` | Alt-текст для accessibility (`""` для декоративных) |
| `glowIntensity` | `'low' \| 'medium' \| 'high' \| 'none'` | Интенсивность зеленого свечения (default: `'medium'`) |
| `glowPosition` | `'center' \| 'top' \| 'bottom' \| 'left' \| 'right'` | Позиция glow-эффекта (default: `'center'`) |
| `animation` | `'none' \| 'float' \| 'float-slow' \| 'pulse' \| 'rotate-slow'` | Анимация (default: `'none'`) |
| `priority` | `boolean` | Eager loading для LCP (hero images) (default: `false`) |
| `class` | `string` | Дополнительные CSS-классы |

---

## 🖼️ OG Images (Open Graph)

### Структура папок

```
website/public/assets/images/og/
├── default.png                 ← Дефолтное OG-изображение (главная)
├── about.png                   ← Страница "Обо мне"
├── projects.png                ← Список проектов
├── links.png                   ← Страница Links
└── blog/                       ← OG для блог-постов (Stage 3)
    ├── post-slug-1.png
    └── post-slug-2.png
```

### Технические требования

| Параметр | Значение |
|----------|----------|
| **Формат** | PNG (для совместимости) или JPG |
| **Размер** | 1200 × 630 px (обязательно!) |
| **Соотношение** | 1.91:1 |
| **Размер файла** | < 300 KB |
| **Safe zone** | 1080 × 560 px (центральная область) |

### Дизайн OG-изображений

**Шаблон для всех OG-изображений:**

```
┌──────────────────────────────────────────────────────────┐
│  Logo/Icon                                   Art Frost   │  ← Header
│                                                          │
│                                                          │
│              [ЗАГОЛОВОК СТРАНИЦЫ]                        │  ← Main Title
│              Подзаголовок или описание                   │  ← Subtitle
│                                                          │
│                                                          │
│                                  artfrost.vercel.app     │  ← Footer
└──────────────────────────────────────────────────────────┘
   1200 × 630 px
```

**Требования к дизайну:**

1. **Фон:** Градиент из brand-colors (`#050605` → `#10B981`)
2. **Логотип:** В верхнем левом углу (32×32 px — 48×48 px)
3. **Заголовок:** 48–72px, bold, белый цвет, максимум 2 строки
4. **Подзаголовок:** 24–32px, regular, серый (`#A1A1AA`), 1 строка
5. **URL:** Домен внизу справа, 18px, muted color
6. **Отступы:** Минимум 60px от краёв

### Какие OG-изображения нужны (минимум для MVP)

| Страница | Имя файла | Заголовок | Описание |
|----------|-----------|-----------|----------|
| **Главная** | `default.png` | Art Frost | IT-специалист & Vibe Coder |
| **О себе** | `about.png` | Обо мне | Узнайте больше о моём опыте |
| **Проекты** | `projects.png` | Портфолио | Мои последние проекты |
| **Links** | `links.png` | Все ссылки | Найдите меня в соцсетях |

### Как OG-изображения используются автоматически

**Уже настроено в PageLayout:**

```astro
<!-- src/pages/index.astro -->
<PageLayout
  title="Art Frost"
  description="..."
  image="/assets/images/og/default.png"  ← Указываем OG-изображение
/>
```

**SEOHead автоматически:**
- Подставляет полный URL (`https://artfrost.vercel.app/assets/images/og/default.png`)
- Добавляет `og:image`, `og:image:width`, `og:image:height`
- Добавляет Twitter Card мета-теги

### Проверка OG-превью

После добавления OG-изображений проверь их через:

1. **[opengraph.xyz](https://www.opengraph.xyz/)** — превью для всех платформ
2. **Telegram** — вставь ссылку в любой чат
3. **[Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)**
4. **[Twitter Card Validator](https://cards-dev.twitter.com/validator)**

---

## 📸 Project Images

### Структура папок

```
website/public/assets/images/projects/
├── ai-telegram-bot-preview.webp
├── artfrost-portfolio-preview.webp
├── dev-cli-preview.webp
├── ecommerce-dashboard-preview.webp
└── ...
```

### Технические требования

| Параметр | Значение |
|----------|----------|
| **Формат** | WebP |
| **Размер файла** | < 150 KB |
| **Разрешение** | 800×600 px — 1200×900 px |
| **Соотношение** | 4:3 или 16:9 |

### Naming Convention

```
{project-slug}-preview.webp
```

**Примеры:**
- `ai-telegram-bot-preview.webp`
- `ecommerce-dashboard-preview.webp`

### Как добавить изображение для проекта

1. **Сохрани** изображение в `public/assets/images/projects/`
2. **Открой** JSON-файл проекта в `src/content/projects/`
3. **Обнови** поле `image`:

```json
{
  "title": "AI Telegram Bot",
  "image": "/assets/images/projects/ai-telegram-bot-preview.webp",
  ...
}
```

4. **Проверь** — изображение автоматически отобразится в:
   - `ProjectCard` на странице `/projects`
   - `FeaturedProjects` на главной странице

---

## 🎯 Favicon & Icons

### Структура папок

```
website/public/
├── favicon.svg                 ← Основной favicon (векторный)
├── favicon.ico                 ← Fallback для старых браузеров
├── apple-touch-icon.png        ← iOS home screen icon (180×180)
└── manifest.json               ← PWA manifest (будущее)
```

### Технические требования

| Файл | Размер | Формат | Описание |
|------|--------|--------|----------|
| `favicon.svg` | 32×32 — 64×64 | SVG | Векторная иконка (dark/light adaptive) |
| `favicon.ico` | 32×32 | ICO | Fallback для IE |
| `apple-touch-icon.png` | 180×180 | PNG | iOS home screen |

### Что делать

1. **Создай** `favicon.svg` с логотипом/буквой "A" в зеленом цвете
2. **Конвертируй** в `favicon.ico` через [favicon.io](https://favicon.io/favicon-converter/)
3. **Создай** `apple-touch-icon.png` (180×180 px, PNG)
4. **Положи** все в `website/public/`

**Автоматически подхватится** через `BaseLayout.astro`:

```html
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="alternate icon" type="image/x-icon" href="/favicon.ico" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
```

---

## 🔤 Self-hosted Fonts

### Структура папок

```
website/public/fonts/
├── inter-var-latin.woff2       ← Inter Variable (латиница)
├── roboto-regular.woff2        ← Roboto Regular
├── roboto-medium.woff2         ← Roboto Medium
├── jetbrains-mono-regular.woff2 ← JetBrains Mono
└── README.md                   ← Инструкция по установке
```

### Откуда скачать шрифты

**Inter Variable:**
- Источник: [rsms.me/inter](https://rsms.me/inter/)
- Файл: `inter-var-latin.woff2` (Variable font, Latin subset)

**Roboto:**
- Источник: [Google Fonts Helper](https://gwfh.mranftl.com/fonts/roboto)
- Файлы: `roboto-regular.woff2`, `roboto-medium.woff2`

**JetBrains Mono:**
- Источник: [jetbrains.com/mono](https://www.jetbrains.com/lp/mono/)
- Файл: `jetbrains-mono-regular.woff2`

### Как добавить шрифты

1. **Скачай** WOFF2 файлы (Latin subset для минимального размера)
2. **Положи** в `website/public/fonts/`
3. **Шрифты автоматически подхватятся** через `src/styles/fonts.css`

**Уже настроено:**

```css
/* src/styles/fonts.css */
@font-face {
  font-family: 'Inter';
  src: url('/fonts/inter-var-latin.woff2') format('woff2');
  font-weight: 100 900;
  font-display: swap;
}
```

---

## 🔧 Оптимизация изображений

### Инструменты (macOS)

```bash
# Установка (один раз)
brew install webp pngquant oxipng

# PNG → WebP конвертация
cwebp -q 85 input.png -o output.webp

# PNG оптимизация (lossy)
pngquant --quality=80-95 --speed 1 --force --ext .png input.png

# PNG оптимизация (lossless)
oxipng -o 4 --strip all input.png

# JPG → WebP
cwebp -q 85 photo.jpg -o photo.webp
```

### Чеклист оптимизации

Перед добавлением изображения:

- [ ] Конвертирован в WebP (для 3D assets, project images)
- [ ] Размер файла < 200 KB (3D), < 150 KB (projects), < 300 KB (OG)
- [ ] Правильное разрешение (см. таблицы выше)
- [ ] Сжатие через pngquant + oxipng (для PNG)
- [ ] Проверено на мобильных устройствах

---

## ✅ Финальный чеклист перед деплоем

### 3D Assets

- [ ] `hero-star.webp` — добавлен (или используется ScrollFrameAnimation)
- [ ] `avatar-sphere.webp` — добавлен и подключен в AboutHero
- [ ] Все 3D изображения < 200 KB
- [ ] Прозрачный фон, формат WebP

### OG Images

- [ ] `og/default.png` — создан (1200×630)
- [ ] `og/about.png` — создан
- [ ] `og/projects.png` — создан
- [ ] `og/links.png` — создан
- [ ] Все OG изображения проверены через opengraph.xyz

### Project Images

- [ ] Все проекты имеют preview-изображения
- [ ] Naming convention соблюдён (`{slug}-preview.webp`)
- [ ] Размер файлов < 150 KB

### Icons & Fonts

- [ ] `favicon.svg` — создан
- [ ] `favicon.ico` — создан
- [ ] `apple-touch-icon.png` — создан (180×180)
- [ ] Шрифты скачаны и добавлены в `public/fonts/`

---

## 🆘 Помощь и поддержка

**Если что-то не работает:**

1. Проверь правильность путей (`/assets/3d/`, `/assets/images/og/`)
2. Убедись, что файлы имеют правильные расширения (`.webp`, `.png`)
3. Проверь naming convention (lowercase, kebab-case)
4. Посмотри консоль браузера на ошибки 404

**Полезные команды:**

```bash
# Проверить, какие файлы в папке 3D
ls -lh website/public/assets/3d/

# Проверить размеры OG images
ls -lh website/public/assets/images/og/

# Запустить dev-server
cd website && npm run dev

# Проверить билд
cd website && npm run build
```

---

**Готово! Теперь просто добавь файлы в соответствующие папки, и они автоматически заработают.**
