# Animation Frames

Эта папка содержит кадры для scroll-based анимации на главной странице.

## 📁 Содержимое

**Кадры:** 36 PNG файлов (001-036)  
**Размер:** ~18 MB  
**Формат:** `ezgif-frame-XXX Background Removed.png`

## 🎬 Использование

Кадры используются компонентом `ScrollFrameAnimation.tsx` на главной странице (Hero секция).

```astro
<ScrollFrameAnimation
  client:load
  framesPath="/animationFrames"
  totalFrames={36}
  framePattern="ezgif-frame-{frame} Background Removed.png"
/>
```

## 🔧 Как работает

1. Пользователь скроллит страницу
2. Компонент определяет прогресс скролла (0-100%)
3. Отображается соответствующий кадр анимации
4. Создаётся эффект плавной 3D-анимации

## ⚠️ Важно

- **НЕ удаляйте** эту папку — анимация на сайте сломается
- **НЕ переименовывайте** файлы — используется строгий паттерн
- Папка должна быть в `public/` для доступа после деплоя
- Все кадры загружаются при первом рендере (preload)

## 🎨 Оптимизация (опционально)

Если нужно уменьшить размер:
1. Конвертируйте PNG → WebP: `cwebp frame.png -o frame.webp`
2. Обновите `framePattern` в Hero.astro
3. Используйте меньше кадров (например, 18 вместо 36)

**Текущее состояние:** ✅ Работает, кадры загружены
