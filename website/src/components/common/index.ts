/**
 * Common UI Components
 * Re-export all common components for easy imports
 *
 * @example
 * import { Button, Card, Tag } from '@components/common';
 */

// Note: Astro components cannot be exported from TypeScript files
// Use direct imports in Astro files:
// import Button from '@components/common/Button.astro';

export {};
